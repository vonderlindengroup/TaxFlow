This folder holds the bundled JavaScript libraries.
They are downloaded automatically when you run: npm install

If they are missing, run: node download-libs.js

Required files:
  - xlsx.full.min.js     (~470KB)
  - pdf.min.js           (~300KB)
  - pdf.worker.min.js    (~700KB)
